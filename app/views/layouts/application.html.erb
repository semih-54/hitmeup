<!DOCTYPE html>
<html>
  <head>
    <title>Hitmeupman</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
     <div class="navbar bg-black px-4" id="nav">
        <div class="d-flex align-items-center">
          <a class="navbar-brand" href="/">
            <%= image_tag("target.png", alt: "logo", width: 110, height: 80) %>
            <a class="hmu" href="/"> HMU </a>
          </a>
        </div>

        <div class="ml-auto d-flex align-items-center">
          <% if user_signed_in? %>
            <% if current_user.is_hitman? %>
              <%= link_to "Contracts", my_contracts_path, class: "nav-link_1"%>
              <% else %>
              <%= link_to "Requests",  contracts_path, class: "nav-link_1"%>
              <%= link_to "New contract",  new_contract_path, class: "nav-link_1"%>
            <% end %>

            <%= link_to "Logout", destroy_user_session_path, data: {turbo_method: :delete}, class: "nav-link_2" %>
          <% else %>
            <%= link_to "Log in", new_user_session_path, class: "nav-link_2" %>
          <% end %>
        </div>
      </div>
    <%= yield %>
    <footer>
    <%= render "shared/flashes" %>
    </footer>
  </body>
</html>
